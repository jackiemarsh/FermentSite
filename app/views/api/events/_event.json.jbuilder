json.extract! event, :id, :title, :description, :start_date, :author_id
# json.ownerFirstName event.owner.first_name
# json.ownerLastName event.owner.last_name
# json.picture_url event.picture_url || url_for(event.photo)